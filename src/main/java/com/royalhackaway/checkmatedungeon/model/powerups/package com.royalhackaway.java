package com.royalhackaway.checkmatedungeon.model.powerups;

/*
 * Placeholder file.
 * This file previously contained an accidental duplication of Game's implementation,
 * which caused "duplicate class" and "class Game is public" compilation errors.
 *
 * Keeping a minimal package file (no public classes) prevents the compiler from
 * seeing duplicate Game definitions while preserving the package directory.
 *
 * If you prefer, we can delete this file entirely; keeping it as a harmless placeholder
 * is slightly safer so tools that expect the folder to contain java files won't complain.
 */
